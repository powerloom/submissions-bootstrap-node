version: '3.8'

services:
  bootstrap-node:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${BOOTSTRAP_PORT:-4001}:${BOOTSTRAP_PORT:-4001}"
    env_file:
      - ./.env
    restart: unless-stopped
